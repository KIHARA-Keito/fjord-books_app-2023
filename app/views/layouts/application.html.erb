<!DOCTYPE html>
<html>
  <head>
    <title>BooksApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= params[:action] %>">
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <p class="user_status">
      <% if user_signed_in? %>
        <strong><%= current_user.email %></strong>でログイン中
        <%= link_to 'プロフィールを編集する', edit_user_registration_path, class: '' %> |
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: ''  %>
      <% else %>
        <%= link_to "新規登録", new_user_registration_path, class: ''  %> |
        <%= link_to "ログイン", new_user_session_path, class: ''  %>
      <% end %>
    </p>

    <%= yield %>
  </body>
</html>
