<h1><%= t('views.title.edit') %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.link.show'), @book %> |
  <%= link_to t('views.link.back'), books_path %>
</nav>
